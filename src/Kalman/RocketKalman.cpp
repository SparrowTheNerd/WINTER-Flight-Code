#include "RocketKalman.h"

RocketKalman::RocketKalman(int randomnum) {

    this->currentRandomNum = randomnum;

};

int RocketKalman::init() {
    if(true) {
        return 1;
    } else {
        return 0;
    }
}